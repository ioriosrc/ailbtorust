{
  "name": "lichtblick-rust-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:rust": "playwright test --project=rust",
    "test:nodejs": "playwright test --project=nodejs",
    "test:compare": "playwright test --project=compare",
    "report": "playwright show-report reports/html"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "pixelmatch": "^5.3.0",
    "pngjs": "^7.0.0"
  }
}
