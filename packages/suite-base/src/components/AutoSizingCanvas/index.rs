```rust
use wasm_bindgen::prelude::*;
use web_sys::{window, CanvasRenderingContext2D, Element, HtmlElement};
use std::cell::RefCell;

#[wasm_bindgen(start)]
pub fn main() -> Result<(), JsValue> {
    let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
    
    let ctx = canvas.get_context("2d")?;

    let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
    let mut resize_handler: Option<Box<dyn FnMut()>> = None;
    
    use wasm_bindgen::prelude::*;
    use web_sys::{self, window};
    use std::cell::RefCell;

    #[wasm_bindgen(start)]
    pub fn main() -> Result<(), JsValue> {
        let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
        let ctx = canvas.get_context("2d")?;

        let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
        let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
        use std::cell::RefCell;

        #[wasm_bindgen(start)]
        pub fn main() -> Result<(), JsValue> {
            let canvas = window().create_element_with_type("canvas")?.dyn_into::<HtmlElement>()?;
        
            let ctx = canvas.get_context("2d")?;

            let canvas_ref: RefCell<Option<CanvasRenderingContext2D>> = RefCell::new(None);
            let mut resize_handler: Option<Box<dyn FnMut()>> = None;
        
        use wasm_bindgen::prelude::*;
        use web_sys::{self, window};
