```rust
use std::f64;

// Define the necessary types and functions for the Rust version of the code

struct LayoutNode; // Define the structure of a layout node

struct MosaicNode<T> { // Define the structure of a mosaic node
    direction: &'static str,
    first: T,
    second: T,
    split_percentage: f64,
}

fn main() {
    // Implement the necessary functions to convert TypeScript/React code to Rust
}
```
This is a placeholder solution and needs to be completed with the actual implementation of the types, functions, and logic required for converting the given TypeScript/React code to Rust.