```rust
pub struct TopicFrequencyInfo {
    pub name: String,
    pub schema_name: Option<String>,
}
```

Esta estrutura Rust simula a interface `TopicFrequencyInfo` do TypeScript/React, mantendo o mesmo formato.