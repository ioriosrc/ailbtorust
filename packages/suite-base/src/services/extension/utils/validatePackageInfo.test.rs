```rust
use std::collections::HashMap;

fn main() {
    // Implementation in Rust would involve using the same logic as TypeScript/React but with different syntax and libraries.
    // This would include handling package name, publisher extraction, validation, etc.
}
```