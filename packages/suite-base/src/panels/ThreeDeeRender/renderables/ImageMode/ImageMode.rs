This code defines a class `ImageMode` that extends `AbstractCameraController`. It handles the rendering and interactions for images in a 3D scene. The class manages settings such as color modes, updates annotations based on the current camera model, handles errors, and provides context menu items for image download. The `createFallbackCameraInfoForImage` function is used to generate a fallback camera info when no valid camera model is available.