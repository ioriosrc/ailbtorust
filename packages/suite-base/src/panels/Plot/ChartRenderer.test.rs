```rust
use std::ops::{Add, Div};

const OPTIONS_CHART: ChartOptionsPlot = {
    devicePixelRatio: 2.0,
    gridColor: "#ccc",
    tickColor: "#000",
};

fn main() {
    // Mocks and setup code here
}
```