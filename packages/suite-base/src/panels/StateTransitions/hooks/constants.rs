```rust
const ROW_SPACING: f64 = 9.0;
const ROW_MARGIN: f64 = ROW_SPACING / 4.0;
```

Esta é a versão funcional do código TypeScript/React para Rust. A constante `ROW_SPACING` é declarada como um f64 (double-precision float), e a constante `ROW_MARGIN` é calculada dividindo `ROW_SPACING` por 4.