```rust
use mui::theme::data_display::avatar::Avatar;

fn main() {
  Avatar::default();
}
```

Neste código, não há necessidade de converter o componente porque o TypeScript/React e Rust são linguagens diferentes. No Rust, o exemplo acima demonstra como usar o `Avatar` do Material-UI para criar um avatar sem especificar qualquer argumento padrão.